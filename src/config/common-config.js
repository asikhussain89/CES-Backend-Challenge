/**
 * Export the GITHUB PERSONAL ACCESS TOKEN from this Module
 * Add your GITHUB PERSONAL ACCESS TOKEN instead of <TOKEN> in below code
 * Add any common configuration data of this app inside the below JSON {}
 */
module.exports = {
  GITHUB_PERSONAL_ACCESS_TOKEN: '<TOKEN>',
  GITHUB_API_BASE_DOMAIN: 'https://api.github.com',
}
